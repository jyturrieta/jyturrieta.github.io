import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#64644B'
        },
        secondary: {
            main: '#CECECF',
           
        },
        relaxed: {
            main: '#1F1F1A'
        },
        campo: {
            main: '#141411',
        },
        fondo: {
            main: '#212429'
        }
    }
});

export default theme;